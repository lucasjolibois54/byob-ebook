import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'plus.unsplash.com', 'cdn.prod.website-files.com', 'hebbkx1anhila5yf.public.blob.vercel-storage.com' ],
  }
};

export default nextConfig;
